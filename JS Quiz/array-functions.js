MyModuleConstructor = function(){};

MyModuleConstructor.prototype = {

	// 1
	isArray: function(value) {
		var arr = "[]";
		if value = arr {
			return true;
		}
		else {
			return false;
		}
	},

	// 2
	cloneArray: function(arr1) {
	},

	// 3
	arrayLeft: function(arr, count) {
	},

	// 4
	arrayRight: function(arr, count) {
	},

	// 5
	joinArray: function(arr, joinString) {
	},

	// 6
	dashBetweenEvenDigits: function(numberString) {
	},

	// 7
	sortNumericArray: function(arr) {
	},

	// 8
	mostFrequent: function(arr) {
	},

	// 9
	swapCase: function(str) {
	},

	// 10
	unique: function(arr) {
	},

	// 11
	leapYears: function(startYear, endYear) {
	},

	// 12
	sumByIndex: function(arr1, arr2) {
	},

	// 13
	duplicates: function(arr) {
	},

	// 14
	flatten: function(arr) {
	},

	// 15
	union: function(arr1, arr2) {
	}

}

var Functions = new MyModuleConstructor();
